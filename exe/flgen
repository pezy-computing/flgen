#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))
require 'file_list_generator'

begin
  FileListGenerator::CLI.new.run(ARGV)
rescue FileListGenerator::FileListGeneratorError => e
  abort e.to_s
end
